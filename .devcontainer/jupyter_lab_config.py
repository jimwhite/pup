# Jupyter Lab configuration for development container
# This allows running 'jupyter lab' without command-line flags

c.ServerApp.ip = "0.0.0.0"
c.ServerApp.open_browser = False
c.IdentityProvider.token = "MY_TOKEN"
