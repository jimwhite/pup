# pup
PUP: the Program Understanding Program

* Add to `sudo apt-get update && sudo apt-get install git-lfs` for acl2

* Also need `pip install z3-solver` for books using SMTLink

* Aaand `pip install external/acl2-mcp`
